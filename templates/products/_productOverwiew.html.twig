<a href="{{ path('product.show', {'id':product.id}) }}" class="box-container container-3 container-tab-4 container-pho-6">
    <div class="box">
        {#<div class="box-header icon-img">
            <img src="assets/img/set.icon.png" alt="">
        </div>#}
        <div class="box-header crop-img crop-img150">
            {% if product.imageName %}
                <img src="{{ vich_uploader_asset(product, 'imageFile') }}" alt="" class="card-img-top">
            {% else %}
                <img src="{{ asset('build/images/empty.jpg') }}" alt="" class="card-img-top">
            {% endif %}
        </div>
        <div class="box-body">
            <div class="box-title-medium">
                {{ product.nom }}
            </div>
            <div class="box-content">
                {{ product.description | nl2br }}
            </div>
            <div class="box-footer">{{ product.prixHT }} €</div>
        </div>
    </div>
</a>