{{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
{{ form_row(form.qte) }}
<div class="text-align-right">
    {{ form_row(form.save) }}
    {{ form_row(form.cancel) }}
</div>
{{ form_end(form) }}
